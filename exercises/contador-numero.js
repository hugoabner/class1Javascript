// 3️⃣ Contador del 1 al N
// Usa un for para imprimir los números del 1 hasta un número N recibido como parámetro.

function contadorNumero(number) {
  for (let index = 0; index < number; index++) {
	console.log(index + 1); // 1, 2, 3, 4, 5
  }
}

contadorNumero(5); // Imprime del 1 al 5